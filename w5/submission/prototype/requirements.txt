# Core runtime
anthropic>=0.40.0      # live Haiku/Sonnet path (optional — prototype runs offline without it)

# Web demo backend (server.py -> the React UI)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Dev / validation
pytest>=8.0.0
